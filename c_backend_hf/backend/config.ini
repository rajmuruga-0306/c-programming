# Hostel Room Allotment System — Configuration
# ------------------------------------------------

# HTTP server port
port=8080

# Path to SQLite database file (relative to backend/ directory)
db_path=../database/hostel.db

# Path to frontend static files (relative to backend/ directory)
frontend_path=../frontend

# Path to SQL schema file for initial setup
schema_path=../database/schema.sql
